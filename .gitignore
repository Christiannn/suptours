node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# Self-hosted Supabase runtime state (never committed — see deploy/README.md)
deploy/supabase/upstream/volumes/db/data/
deploy/supabase/upstream/volumes/storage/
deploy/**/.env

# Which environment a clone on the VPS belongs to. Written by provision.sh;
# belongs to the clone, not the branch.
.deploy-env

# Off-box database and storage backups pulled down by Invoke-Backup.ps1.
# These contain real customer data; this repo is public. Never commit them.
/backups/
*.sql.gz
*-storage.tar.gz
