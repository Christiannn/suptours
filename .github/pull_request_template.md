## What this changes

<!-- One or two sentences. What is different afterwards, and why. -->

## How it was tested

<!-- Locally against Docker Supabase? On staging? Which pages or flows? -->

---

- [ ] Targets `develop` (not `staging` or `main` — those are promoted, never merged into)
- [ ] If it adds a migration: it is a **new** file, not an edit to an existing one
- [ ] If it changes anything under `deploy/`: say so here, it needs a staging run first
